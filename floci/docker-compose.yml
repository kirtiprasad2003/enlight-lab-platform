# AWS local emulator for Demo 4 (Drift & Cost) — NOT for Kubernetes/EKS
# See Floci_Setup_Guide.pdf
services:
  floci:
    image: floci/floci:latest
    ports:
      - "4566:4566"
    volumes:
      - floci-data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock

volumes:
  floci-data:
